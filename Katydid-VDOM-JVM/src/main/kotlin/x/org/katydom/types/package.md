# Package x.org.katydom.types

This package defines basic types (type aliases) that are part of the Katydid API but are platform dependent.

For the JVM these types are generally aliases for JDK types.

